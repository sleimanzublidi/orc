# Self-improvement workflow — generates product and engineering ideas, debates them,
# implements the best, then runs a fix-loop to clean up.
#
# Usage:
#   orc start self-improve
name: self-improve
description: Autonomous self-improvement cycle that generates product and engineering ideas, debates and ranks them, implements the top picks, then runs iterative code review.

nodes:
  - id: setup
    command: |
      DATE=$(date +%Y-%m-%d)
      BRANCH="self-improve/$DATE"
      git checkout -b "$BRANCH" 2>/dev/null || git checkout "$BRANCH"
      mkdir -p {{workspace}}/self-improve
      LAST=$(ls {{workspace}}/self-improve/*-${DATE}-*.md 2>/dev/null | sed 's/.*-\([0-9]*\)\.md/\1/' | sort -n | tail -1)
      ITERATION=$(( ${LAST:-0} + 1 ))
      echo "${DATE}:${ITERATION}"
    output: run_info

  - id: codebase-summary
    agent: claude-code
    depends_on: setup
    prompt: |
      Produce a concise codebase summary for downstream agents.

      1. Read CLAUDE.md, README.md, and the specs in Docs/Specs/.
      2. List every SPM target with a one-line purpose.
      3. Summarize CLI commands and workflow features currently available.
      4. Run `swift build 2>&1 | tail -5` and `swift test 2>&1 | tail -10` — report pass/fail and any warnings.
      5. Run `git log main..HEAD --oneline` — list recent branch changes.
      6. List any known gaps, TODOs, or incomplete features you notice.

      Output a single markdown document (do NOT save to disk).
    output: codebase_summary

  - id: product-thinker
    agent: claude-code
    depends_on: codebase-summary
    prompt: |
      You are a product thinker evaluating the product built in this repository.

      Here is a codebase summary prepared by a prior agent — use it instead of exploring from scratch:

      {{codebase_summary}}

      Read previous idea files in `{{workspace}}/self-improve/` and `git log main..HEAD --oneline` to see what was already proposed or implemented — do NOT re-propose those ideas.

      If README.md contains a roadmap or planned features section, read it and do NOT propose ideas already listed there.

      Produce a top 3 ranked product ideas or suggestions. Focus on:
      - User experience improvements
      - Missing features that would make the product more useful
      - Workflow ergonomics and discoverability
      - Error messages and user guidance
      - Documentation gaps from a user perspective

      Rank by impact (how much value it delivers to users) and feasibility.

      Format as a markdown file with:
      ```
      # Product Ideas — <date>  (Iteration <N>)

      ## 1. <Title>
      **Impact:** High/Medium/Low
      **Effort:** High/Medium/Low
      **Description:** ...
      **Rationale:** ...

      (repeat for each idea)
      ```

      Parse the run info "{{run_info}}" as DATE:ITERATION (split on colon).
      Save the file to `{{workspace}}/self-improve/product-ideas-<DATE>-<ITERATION>.md`.

      Output the full path of the saved file.
    output: product_ideas_file

  - id: principal-engineer
    agent: claude-code
    depends_on: codebase-summary
    prompt: |
      You are a principal engineer evaluating the architecture and technical implementation of this repository.

      Here is a codebase summary prepared by a prior agent — use it instead of exploring from scratch:

      {{codebase_summary}}

      Read previous idea files in `{{workspace}}/self-improve/` and `git log main..HEAD --oneline` to see what was already proposed or implemented — do NOT re-propose those ideas.

      If README.md contains a roadmap or planned features section, read it and do NOT propose ideas already listed there.

       Produce a top 3 ranked engineering ideas about architecture and technical implementation. Focus on:
      - Architectural improvements (module boundaries, dependency flow, protocol design)
      - Performance opportunities (concurrency, caching, I/O)
      - Reliability and robustness (error handling, edge cases, resilience)
      - Code quality (duplication, complexity, testability)
      - Developer experience (build times, test ergonomics, debugging)

      Rank by technical impact and risk.

      Format as a markdown file with:
      ```
      # Engineering Ideas — <date>  (Iteration <N>)

      ## 1. <Title>
      **Impact:** High/Medium/Low
      **Risk:** High/Medium/Low
      **Description:** ...
      **Rationale:** ...

      (repeat for each idea)
      ```

      Parse the run info "{{run_info}}" as DATE:ITERATION (split on colon).
      Save the file to `{{workspace}}/self-improve/engineer-ideas-<DATE>-<ITERATION>.md`.

      Output the full path of the saved file.
    output: engineer_ideas_file

  - id: adversarial-reviewer
    agent: claude-code
    depends_on:
      - product-thinker
      - principal-engineer
    prompt: |
      You are an adversarial reviewer. Your job is to critically evaluate and debate the quality of proposed ideas, then select only the best for implementation.

      Read the two idea files:
      - {{product_ideas_file}}
      - {{engineer_ideas_file}}

      Also read CLAUDE.md and the specs in Docs/Specs/ to ground your evaluation in the project's actual goals and constraints.

      For each idea across both files, evaluate:
      1. **Is it actually a problem?** — Does evidence in the codebase support this being a real issue, or is it speculative?
      2. **Is the proposed approach sound?** — Are there simpler alternatives? Does it conflict with existing design decisions?
      3. **Is it worth the effort?** — Given the project's current stage, is this a priority or a distraction?
      4. **Risk assessment** — Could this introduce regressions, break existing workflows, or add unnecessary complexity?

      Be skeptical. Challenge assumptions. Push back on vague or low-impact suggestions.

      After your analysis, re-rank ALL ideas from both files combined and select the single best one to implement. It should be clearly valuable, feasible, and low-risk.

      Format as a markdown file with:
      ```
      # Task Selection — <date>  (Iteration <N>)

      ## Debate Summary
      (Brief overview of your evaluation process and key arguments)

      ## Rejected Ideas
      ### <Title> (from product/engineering)
      **Reason:** ...

      (repeat for each rejected idea)

      ## Selected Task
      ### <Title>
      **Source:** Product Ideas #N / Engineering Ideas #N
      **Priority:** P0/P1/P2
      **Description:** ...
      **Implementation notes:** ...
      ```

      Parse the run info "{{run_info}}" as DATE:ITERATION (split on colon).
      Save the file to `{{workspace}}/self-improve/tasks-<DATE>-<ITERATION>.md`.

      Output the full path of the saved file.
    output: tasks_file

  - id: senior-engineer
    agent: claude-code
    depends_on: adversarial-reviewer
    prompt: |
      You are a senior engineer. Your job is to plan and implement the single selected task.

      Read the task file: {{tasks_file}}
      Read CLAUDE.md for coding conventions and commit requirements.
      Read the specs in Docs/Specs/ for design context.

      For the selected task:

      1. **Plan**: Identify the files to change, the approach, and any risks.
      2. **Implement**: Make the changes. Follow all project conventions from CLAUDE.md.
      3. **Build**: Run `bash Scripts/build.sh` to verify the build passes.
      4. **Test**: Run `swift test` to verify tests pass. Add tests for new functionality.
      5. **Commit**: Create ONE commit with message format: `[Claude] <description of what changed>`
         Stage ALL modified files.

      If the task turns out to be more complex than expected or would require breaking changes, output ONLY `SKIPPED` and document why.

      Otherwise, output a summary of what was implemented and committed.
    output: implementation_result

  - id: fix-loop
    workflow: .orc/workflows/fix-loop.yaml
    depends_on: senior-engineer
    when: "{{implementation_result}} != 'SKIPPED'"

output:
  product_ideas: "{{product_ideas_file}}"
  engineer_ideas: "{{engineer_ideas_file}}"
  tasks: "{{tasks_file}}"
  implementation: "{{implementation_result}}"
  review: "{{fix-loop.review}}"
