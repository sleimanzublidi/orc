# Implement a task from a task file — plan, code, build, test, commit.
#
# Usage:
#   orc start implement-task --input task_file=".orc/self-improve/tasks-20260411-110827-1.md"
#   orc start implement-task --input task_file=".orc/self-improve/tasks-20260411-110827-1.md" --input agent="copilot"
#   orc start implement-task --input task_file=".orc/self-improve/tasks-20260411-110827-1.md" --input worktree_path="/path/to/worktree"
name: implement-task
description: Plans and implements a single task from a task file, then builds, tests, and commits.

input:
  - name: agent
    type: string
    default: "claude-code"
  - name: task_file
    type: file
    required: true
  - name: worktree_path
    type: string
    default: "{{repo_root}}"

nodes:
  - id: implement
    agent: "{{agent}}"
    prompt_file: "{{orc_root}}/prompts/implement-task.md"
    parameters:
      permission_mode: dontAsk
    output: result

output:
  implementation: "{{result}}"
