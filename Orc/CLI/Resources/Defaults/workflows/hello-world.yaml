# A simple hello-world workflow to verify your Orc setup.
name: hello-world
description: Prints a greeting to verify Orc is working.

input:
  - name: greeting
    type: string
    required: false

nodes:
  - id: greet
    agent: shell
    prompt: "echo '{{greeting | default: Hello, World!}}'"
